# hk-009 · HTML audit report  *(archived)*

> Archived detail. Rotated out of FEATURES.md when it closed. Grep here for the
> full story; the summary table only carries the one-line row.

- **Status:** ✅ done · closed 2026-07-19 · **Depends on:** hk-004

## Done when (all met)

`audit.mjs --html FILE` writes a single self-contained report — no external requests, readable
in light and dark mode, showing every check with its fix line.

## Evidence

| ✓ | Check | By | Proof |
|:-:|-------|----|-------|
| ✅ | --html flag writes a report | Jovanes Jovanotti | `audit.mjs --target . --html out.html` → 9.7 KB file |
| ✅ | Fully self-contained | Jovanes Jovanotti | Zero matches for `http://`, `https://`, `src=` or `href=` — nothing loads externally |
| ✅ | Renders pass, fail and warn | Jovanes Jovanotti | Corrupted fixture produced 3 FAIL + 28 PASS rows, each failure carrying its fix line |
| ✅ | Untrusted content escaped | Jovanes Jovanotti | Parsed repo content is HTML-escaped (6 `&amp;` entities in output) |
| ✅ | Dark mode supported | Jovanes Jovanotti | `prefers-color-scheme` block defines a full second palette |
| 🟡 | Visually reviewed in a browser | — | Deferred — the preview pane would not load `file://` URLs; verified structurally instead |

## Decisions

- The report renders repo-derived strings (file names, parsed table cells), so every
  interpolated value is escaped. A harness audit should not be an injection vector.
- No charting library, no CDN. A report you cannot open offline is not a report.

## Note

The visual check is deliberately left 🟡 rather than claimed. Structure, escaping and
self-containment are verified; how it actually *looks* in a browser is not.
